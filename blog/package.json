{
  "name": "aidemo-blog",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Source for the aidemo blog (https://aidemo.top/blog/): article JSON, blog.config.json, style overrides, topic map. The static site is generated by the centralized blog-engine (github:tandryukha/blog-engine) — there are no local bake/validate scripts anymore; see README.md.",
  "scripts": {
    "validate": "npx -y github:tandryukha/blog-engine#stable validate --config blog.config.json",
    "bake": "npx -y github:tandryukha/blog-engine#stable bake --config blog.config.json --out ../docs/blog",
    "preview": "python3 -m http.server 8090 --directory ../docs"
  }
}
